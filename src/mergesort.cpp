#include "mergesort.h"
#include <vector>
void Mergesort::run() {
    if(m_tape){
        std::vector<int> storage;
        if(m_tape->filesize() > 0){}
        //storage.reserve(1'073'741'824); //1 GB
        storage.size();
    }
}
